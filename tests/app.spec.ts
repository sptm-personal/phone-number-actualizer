describe('App', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('placeholder test', () => {
    it('should always pass', () => {
      expect(true).toEqual(true);
    });
  });
});
